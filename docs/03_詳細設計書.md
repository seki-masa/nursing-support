# 詳細設計書

## 1. コンポーネント詳細設計

### 1.1 Sidebar / CareRecipientList（左サイドバー）

**責務**: 介護対象者の一覧をステータス別グループで表示し、選択状態を管理する

**状態**:
```typescript
const [selectedId, setSelectedId] = useState<string | null>(null)
const [searchQuery, setSearchQuery] = useState('')
```

**表示仕様**:
- ステータス危険度降順でグループ化表示（危篤→重篤→要注意→経過観察→健康→死亡→退院）
- 各グループは折りたたみ可能（デフォルト展開）
- 名前・ふりがなでのインクリメンタル検索
- 選択中の対象者はハイライト表示
- 各項目にステータスバッジ（色付き円）表示

**インターフェース**:
```typescript
interface CareRecipientListItem {
  id: string
  name: string
  nameKana: string
  status: CareStatus
  room: string | null
}
```

---

### 1.2 StatusBadge

**責務**: ステータスを色付きバッジで視覚表示する

**カラーマッピング**:
```typescript
const STATUS_CONFIG = {
  CRITICAL:    { label: '危篤',   color: 'bg-red-600 text-white',     ring: 'ring-red-600' },
  SEVERE:      { label: '重篤',   color: 'bg-orange-600 text-white',  ring: 'ring-orange-600' },
  CAUTION:     { label: '要注意', color: 'bg-yellow-600 text-white',  ring: 'ring-yellow-600' },
  OBSERVATION: { label: '経過観察', color: 'bg-blue-600 text-white', ring: 'ring-blue-600' },
  HEALTHY:     { label: '健康',   color: 'bg-green-600 text-white',   ring: 'ring-green-600' },
  DECEASED:    { label: '死亡',   color: 'bg-gray-500 text-white',    ring: 'ring-gray-500' },
  DISCHARGED:  { label: '退院',   color: 'bg-gray-400 text-white',    ring: 'ring-gray-400' },
} as const
```

---

### 1.3 VitalDashboard（バイタルダッシュボード）

**責務**: 選択された介護対象者の最新バイタルと時系列グラフを表示する

**レイアウト**:
```
┌───────────────────────────────────────────┐
│  田中 太郎 様  [健康] 301号室              │
│  [プロフィール表示]  [バイタル入力]         │
├───────────────────────────────────────────┤
│  最新バイタル  記録: 2024/01/15 09:30      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │血圧  │ │心拍  │ │体温  │ │SpO2 │    │
│  │135/85│ │72   │ │36.5 │ │98%  │    │
│  │⚠高め│ │正常  │ │正常  │ │正常  │    │
│  └──────┘ └──────┘ └──────┘ └──────┘    │
│  ┌──────┐ ┌──────┐ ...                   │
│  │呼吸数│ │体重  │                        │
│  │16   │ │62.5 │                        │
│  └──────┘ └──────┘                       │
├───────────────────────────────────────────┤
│  [血圧][心拍][体温][SpO2][体重]  ← タブ    │
│                                           │
│  [折れ線グラフ - 過去30日]                 │
└───────────────────────────────────────────┘
```

---

### 1.4 VitalCard（バイタル値カード）

**責務**: 個別のバイタル値を表示し、異常値を視覚的にハイライトする

**異常値判定**:
```typescript
export const VITAL_RANGES: Record<string, { min: number; max: number; unit: string; label: string }> = {
  systolicBp:       { min: 90,   max: 139,  unit: 'mmHg', label: '収縮期血圧' },
  diastolicBp:      { min: 60,   max: 89,   unit: 'mmHg', label: '拡張期血圧' },
  heartRate:        { min: 60,   max: 100,  unit: 'bpm',  label: '心拍数' },
  respiratoryRate:  { min: 12,   max: 20,   unit: '回/分', label: '呼吸数' },
  temperature:      { min: 36.0, max: 37.4, unit: '℃',    label: '体温' },
  spo2:             { min: 96,   max: 100,  unit: '%',    label: 'SpO2' },
  bloodSugar:       { min: 70,   max: 140,  unit: 'mg/dL', label: '血糖値' },
  painScore:        { min: 0,    max: 3,    unit: '',     label: '疼痛スコア' },
}

export function isAbnormal(field: string, value: number): boolean {
  const range = VITAL_RANGES[field]
  if (!range) return false
  return value < range.min || value > range.max
}

export function getAbnormalLevel(field: string, value: number): 'normal' | 'warning' | 'danger' {
  const range = VITAL_RANGES[field]
  if (!range) return 'normal'
  const deviation = Math.max(
    (range.min - value) / range.min,
    (value - range.max) / range.max,
    0
  )
  if (deviation > 0.2) return 'danger'
  if (deviation > 0) return 'warning'
  return 'normal'
}
```

**カードスタイル**:
- `normal`: 白背景、グレーボーダー
- `warning`: 黄背景、黄ボーダー、⚠ アイコン
- `danger`: 赤背景、赤ボーダー、🔴 アイコン

---

### 1.5 VitalInputForm（バイタル入力フォーム）

**バリデーションスキーマ（Zod）**:
```typescript
const vitalSchema = z.object({
  recordedAt: z.string().optional(),
  status: z.enum(['CRITICAL', 'SEVERE', 'CAUTION', 'OBSERVATION', 'HEALTHY', 'DECEASED', 'DISCHARGED']).optional().nullable(),
  deceasedAt: z.string().optional().nullable(),
  dischargedAt: z.string().optional().nullable(),
  systolicBp: z.number().int().min(40).max(300).optional().nullable(),
  diastolicBp: z.number().int().min(20).max(200).optional().nullable(),
  heartRate: z.number().int().min(20).max(300).optional().nullable(),
  respiratoryRate: z.number().int().min(0).max(60).optional().nullable(),
  temperature: z.number().min(30).max(45).optional().nullable(),
  spo2: z.number().int().min(50).max(100).optional().nullable(),
  weight: z.number().min(1).max(300).optional().nullable(),
  bloodSugar: z.number().int().min(20).max(600).optional().nullable(),
  consciousnessLevel: z.number().int().min(0).max(300).optional().nullable(),
  painScore: z.number().int().min(0).max(10).optional().nullable(),
  edema: z.enum(['NONE', 'MILD', 'MODERATE', 'SEVERE']).optional().nullable(),
  urineOutput: z.number().int().min(0).max(5000).optional().nullable(),
  notes: z.string().max(1000).optional().nullable(),
})
```

**UI仕様**:
- 全項目が任意入力（少なくとも1項目入力を推奨）
- 記録日時はデフォルト現在日時（変更可能）
- ステータス（CareStatus）を記録時に選択可能。死亡/退院選択時は日時も記録する
- 「デバイスから取得」ボタン（将来実装、現状はdisabled + ツールチップ表示）

> 介護対象者の現在ステータスは別カラムではなく、最新バイタルの `status` から導出する。

---

### 1.6 ProfileEditForm（プロフィール編集フォーム）

**持病・アレルギーのタグUI仕様**:
- テキスト入力後 Enter または「追加」ボタンで追加
- 追加済み項目は × ボタン付きタグで表示
- 重複チェック（同一名は登録不可）

**家族管理UI仕様**:
- 既存家族の検索・選択（名前で検索）
- 新規家族の作成（別フォーム展開）
- 紐付け済み家族の解除（× ボタン）

---

## 2. Prismaスキーマ

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum BloodType {
  A_PLUS
  A_MINUS
  B_PLUS
  B_MINUS
  O_PLUS
  O_MINUS
  AB_PLUS
  AB_MINUS
}

enum CareStatus {
  CRITICAL      // 危篤
  SEVERE        // 重篤
  CAUTION       // 要注意
  OBSERVATION   // 経過観察
  HEALTHY       // 健康
  DECEASED      // 死亡
  DISCHARGED    // 退院
}

enum Edema {
  NONE
  MILD
  MODERATE
  SEVERE
}

enum InputMethod {
  MANUAL
  WEARABLE
}

enum Role {
  ADMIN
  STAFF
}

model Business {
  id          String          @id @default(cuid())
  code        String          @unique // 事業者ID（短い英数字コード）
  companyName String
  address     String
  contactName String
  phone       String
  email       String
  users       User[]
  recipients  CareRecipient[]
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
}

model User {
  id           String   @id @default(cuid())
  name         String
  email        String   @unique
  passwordHash String
  role         Role     @default(STAFF)
  businessId   String
  business     Business @relation(fields: [businessId], references: [id])
  vitals       Vital[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model CareRecipient {
  id                String                @id @default(cuid())
  name              String
  nameKana          String
  gender            Gender
  birthDate         DateTime
  bloodType         BloodType?
  room              String?
  notes             String?
  deletedAt         DateTime?
  businessId        String
  business          Business              @relation(fields: [businessId], references: [id])
  families          CareRecipientFamily[]
  medicalConditions MedicalCondition[]
  allergies         Allergy[]
  vitals            Vital[]
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt
}

model Family {
  id           String                @id @default(cuid())
  name         String
  relationship String
  phone        String?
  email        String?
  notes        String?
  recipients   CareRecipientFamily[]
  createdAt    DateTime              @default(now())
  updatedAt    DateTime              @updatedAt
}

model CareRecipientFamily {
  careRecipientId String
  familyId        String
  careRecipient   CareRecipient @relation(fields: [careRecipientId], references: [id], onDelete: Cascade)
  family          Family        @relation(fields: [familyId], references: [id], onDelete: Cascade)

  @@id([careRecipientId, familyId])
}

model MedicalCondition {
  id              String        @id @default(cuid())
  careRecipientId String
  careRecipient   CareRecipient @relation(fields: [careRecipientId], references: [id], onDelete: Cascade)
  name            String
  createdAt       DateTime      @default(now())
}

model Allergy {
  id              String        @id @default(cuid())
  careRecipientId String
  careRecipient   CareRecipient @relation(fields: [careRecipientId], references: [id], onDelete: Cascade)
  name            String
  createdAt       DateTime      @default(now())
}

model Vital {
  id                 String        @id @default(cuid())
  careRecipientId    String
  careRecipient      CareRecipient @relation(fields: [careRecipientId], references: [id], onDelete: Cascade)
  recordedAt         DateTime      @default(now())
  recordedBy         String
  recorder           User          @relation(fields: [recordedBy], references: [id])
  systolicBp         Int?
  diastolicBp        Int?
  heartRate          Int?
  respiratoryRate    Int?
  temperature        Decimal?      @db.Decimal(4, 1)
  spo2               Int?
  weight             Decimal?      @db.Decimal(5, 2)
  bloodSugar         Int?
  consciousnessLevel Int?
  painScore          Int?
  edema              Edema?
  urineOutput        Int?
  inputMethod        InputMethod   @default(MANUAL)
  status             CareStatus?   // この記録時点のステータス（対象者の現ステータスは最新値から導出）
  deceasedAt         DateTime?
  dischargedAt       DateTime?
  notes              String?
  createdAt          DateTime      @default(now())

  @@index([careRecipientId, recordedAt(sort: Desc)])
}
```

---

## 3. 型定義

```typescript
// types/index.ts
import type { CareStatus, Gender, BloodType, Edema, InputMethod, Role } from '@prisma/client'

export type { CareStatus, Gender, BloodType, Edema, InputMethod, Role }

export interface CareRecipientListItem {
  id: string
  name: string
  nameKana: string
  status: CareStatus | null  // 最新バイタルのステータス。バイタル未記録はnull
  room: string | null
}

export interface CareRecipientDetail {
  id: string
  name: string
  nameKana: string
  gender: Gender
  birthDate: string
  bloodType: BloodType | null
  room: string | null
  status: CareStatus | null   // 最新バイタルから導出
  deceasedAt: string | null   // 最新の死亡ステータスバイタルから導出
  dischargedAt: string | null
  updatedAt: string
  notes: string | null
  medicalConditions: { id: string; name: string }[]
  allergies: { id: string; name: string }[]
  families: {
    id: string
    name: string
    relationship: string
    phone: string | null
    email: string | null
  }[]
}

export interface VitalRecord {
  id: string
  recordedAt: string
  recorder: { id: string; name: string }
  status: CareStatus | null
  deceasedAt: string | null
  dischargedAt: string | null
  systolicBp: number | null
  diastolicBp: number | null
  heartRate: number | null
  respiratoryRate: number | null
  temperature: number | null
  spo2: number | null
  weight: number | null
  bloodSugar: number | null
  consciousnessLevel: number | null
  painScore: number | null
  edema: Edema | null
  urineOutput: number | null
  inputMethod: InputMethod
  notes: string | null
}
```

---

## 4. 認証設定

```typescript
// lib/auth.ts
import { NextAuthOptions } from 'next-auth'
import CredentialsProvider from 'next-auth/providers/credentials'
import bcrypt from 'bcryptjs'
import { prisma } from './prisma'

export const authOptions: NextAuthOptions = {
  providers: [
    CredentialsProvider({
      name: 'credentials',
      credentials: {
        email: { label: 'メールアドレス', type: 'email' },
        password: { label: 'パスワード', type: 'password' },
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) return null
        const user = await prisma.user.findUnique({
          where: { email: credentials.email },
        })
        if (!user) return null
        const valid = await bcrypt.compare(credentials.password, user.passwordHash)
        if (!valid) return null
        return { id: user.id, name: user.name, email: user.email, role: user.role, businessId: user.businessId }
      },
    }),
  ],
  callbacks: {
    jwt({ token, user }) {
      if (user) {
        token.id = user.id
        token.role = (user as any).role
        token.businessId = (user as any).businessId
      }
      return token
    },
    session({ session, token }) {
      if (session.user) {
        const u = session.user as { id?: string; role?: string; businessId?: string }
        u.id = token.id as string
        u.role = token.role as string
        u.businessId = token.businessId as string
      }
      return session
    },
  },
  pages: {
    signIn: '/login',
  },
  session: { strategy: 'jwt' },
}
```

---

## 5. 実装ファイル一覧

| ファイル | 役割 |
|---------|------|
| `prisma/schema.prisma` | DBスキーマ定義 |
| `prisma/seed.ts` | 初期データ（管理者ユーザ・サンプルデータ） |
| `app/(auth)/login/page.tsx` | ログイン画面 |
| `app/(auth)/register/business/page.tsx` | 事業者登録画面 |
| `app/(auth)/register/account/page.tsx` | アカウント新規登録画面 |
| `app/(dashboard)/layout.tsx` | 認証チェック + サイドバーレイアウト |
| `app/(dashboard)/dashboard/page.tsx` | ダッシュボード（`/dashboard?id=xxx`） |
| `app/page.tsx` | ルート（`/dashboard` へリダイレクト） |
| `app/(dashboard)/care-recipients/new/page.tsx` | 新規登録フォーム |
| `app/(dashboard)/care-recipients/[id]/page.tsx` | プロフィール表示 |
| `app/(dashboard)/care-recipients/[id]/edit/page.tsx` | プロフィール編集 |
| `app/(dashboard)/care-recipients/[id]/vitals/new/page.tsx` | バイタル入力 |
| `app/(dashboard)/users/page.tsx` | ユーザ一覧（管理者） |
| `app/(dashboard)/users/new/page.tsx` | ユーザ新規作成（管理者） |
| `app/(dashboard)/users/[id]/page.tsx` | ユーザ詳細 |
| `app/(dashboard)/users/[id]/edit/page.tsx` | ユーザ編集（パスワード変更は本人のみ） |
| `app/api/auth/[...nextauth]/route.ts` | NextAuth APIルート |
| `app/api/businesses/route.ts` | 事業者登録（事業者ID発行・メール送信） |
| `app/api/register/route.ts` | アカウント新規登録（ADMIN/STAFF自動判定） |
| `app/api/care-recipients/route.ts` | 対象者一覧・新規作成 |
| `app/api/care-recipients/[id]/route.ts` | 対象者詳細・更新・削除 |
| `app/api/care-recipients/[id]/vitals/route.ts` | バイタル一覧・作成 |
| `app/api/care-recipients/[id]/families/route.ts` | 家族紐付け |
| `app/api/care-recipients/[id]/families/[familyId]/route.ts` | 家族紐付け解除 |
| `app/api/families/route.ts` | 家族一覧・新規作成 |
| `app/api/families/[id]/route.ts` | 家族更新 |
| `app/api/users/route.ts` | ユーザ一覧・新規作成（管理者） |
| `app/api/users/[id]/route.ts` | ユーザ詳細・更新・削除（パスワード変更は本人のみ） |
| `components/sidebar/Sidebar.tsx` | サイドバー全体 |
| `components/sidebar/CareRecipientList.tsx` | 介護対象者一覧 |
| `components/sidebar/StatusBadge.tsx` | ステータスバッジ |
| `components/vitals/VitalDashboard.tsx` | バイタルダッシュボード |
| `components/vitals/VitalInputForm.tsx` | バイタル入力フォーム |
| `components/vitals/VitalChart.tsx` | 時系列グラフ（Recharts） |
| `components/vitals/VitalCard.tsx` | バイタル値カード |
| `components/profile/ProfileView.tsx` | プロフィール表示 |
| `components/profile/ProfileEditForm.tsx` | プロフィール編集フォーム |
| `components/profile/FamilySection.tsx` | 家族一覧・管理 |
| `lib/prisma.ts` | Prismaクライアント（シングルトン） |
| `lib/auth.ts` | NextAuth設定（businessId伝播） |
| `lib/business-code.ts` | 事業者IDコード生成 |
| `lib/email.ts` | 事業者ID送信メール（Nodemailer / SMTP） |
| `lib/vitals-utils.ts` | バイタル正常範囲ロジック |
| `types/index.ts` | 共通型定義 |

---

## 6. 検証チェックリスト

### 6.1 認証
- [ ] 未認証でダッシュボードにアクセスするとログイン画面にリダイレクト
- [ ] 正しいメール/パスワードでログイン成功
- [ ] 誤ったパスワードでエラーメッセージ表示
- [ ] ログアウトで認証セッション破棄

### 6.1.1 事業者・アカウント登録（マルチテナント）
- [ ] 事業者登録で事業者IDが発行され、完了画面に表示される
- [ ] 事業者IDが入力メールアドレスへ送信される（SMTP未設定時はサーバログ出力）
- [ ] その事業者IDで登録した1人目がADMIN、2人目以降がSTAFFになる
- [ ] 存在しない事業者ID／メール重複／パスワード不一致でエラー
- [ ] ログイン後、自身の事業者の介護対象者のみ表示される
- [ ] 管理者のユーザ管理に同一事業者のユーザのみ表示される
- [ ] 別事業者のデータが相互に参照できない

### 6.2 介護対象者管理
- [ ] 介護対象者の新規登録（必須項目バリデーション含む）
- [ ] プロフィールの表示・編集
- [ ] 持病の追加・削除
- [ ] アレルギーの追加・削除
- [ ] 家族の新規作成と紐付け
- [ ] 既存家族の検索と紐付け
- [ ] 家族の紐付け解除
- [ ] 論理削除（一覧から非表示）

### 6.3 サイドバー
- [ ] ステータス別グループ表示
- [ ] 危険度降順ソート
- [ ] 名前検索フィルタ
- [ ] 選択状態のハイライト
- [ ] ステータスバッジの色確認

### 6.4 バイタル管理
- [ ] バイタル入力フォームの保存
- [ ] 最新バイタルのカード表示
- [ ] 異常値の赤色ハイライト
- [ ] 時系列グラフの表示・項目切替
- [ ] バイタル記録の削除

### 6.5 ユーザ管理（管理者）
- [ ] 管理者のみユーザ一覧・新規作成・削除が可能
- [ ] 一覧に同一事業者のユーザのみ表示される
- [ ] 自分自身は削除できない
- [ ] ロール変更は管理者のみ可能
- [ ] パスワード変更は本人のみ可能（管理者でも他ユーザのパスワードは変更不可：403）
- [ ] 本人のパスワード変更時に現在のパスワード検証が行われる（不一致でエラー）
- [ ] 他ユーザ編集画面ではパスワード変更欄が表示されない
- [ ] 別事業者のユーザを操作できない（404）

### 6.6 レスポンシブ
- [ ] タブレット幅（768px）で正常表示
- [ ] サイドバーの折りたたみ（モバイル）
