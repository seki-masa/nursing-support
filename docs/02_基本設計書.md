# 基本設計書

## 1. システムアーキテクチャ

### 1.1 技術スタック

| レイヤー | 技術 | バージョン |
|---------|------|-----------|
| フロントエンド | Next.js (App Router) | 14.x |
| 言語 | TypeScript | 5.x |
| UIコンポーネント | shadcn/ui | latest |
| CSSフレームワーク | Tailwind CSS | 3.x |
| グラフ | Recharts | 2.x |
| バックエンド | Next.js API Routes | 14.x |
| ORM | Prisma | 5.x |
| データベース | PostgreSQL | 15.x |
| 認証 | NextAuth.js | 4.x |
| バリデーション | Zod | latest |
| フォーム | React Hook Form | latest |
| メール送信 | Nodemailer (SMTP / Brevo) | 6.x |

### 1.2 ディレクトリ構成

```
nursing-support/
├── app/
│   ├── (auth)/
│   │   ├── login/
│   │   │   └── page.tsx
│   │   ├── register/
│   │   │   ├── business/
│   │   │   │   └── page.tsx        # 事業者登録
│   │   │   └── account/
│   │   │       └── page.tsx        # アカウント新規登録
│   │   └── layout.tsx
│   ├── (dashboard)/
│   │   ├── layout.tsx              # サイドバー + メインレイアウト
│   │   ├── page.tsx                # ダッシュボード（対象者未選択状態）
│   │   └── care-recipients/
│   │       ├── new/
│   │       │   └── page.tsx        # 介護対象者新規登録
│   │       └── [id]/
│   │           ├── page.tsx        # プロフィール表示
│   │           ├── edit/
│   │           │   └── page.tsx    # プロフィール編集
│   │           └── vitals/
│   │               └── new/
│   │                   └── page.tsx # バイタル入力
│   ├── api/
│   │   ├── auth/
│   │   │   └── [...nextauth]/
│   │   │       └── route.ts
│   │   ├── businesses/
│   │   │   └── route.ts            # POST事業者登録（認証不要）
│   │   ├── register/
│   │   │   └── route.ts            # POSTアカウント新規登録（認証不要）
│   │   ├── care-recipients/
│   │   │   ├── route.ts            # GET一覧, POST新規作成
│   │   │   └── [id]/
│   │   │       ├── route.ts        # GET詳細, PATCH更新, DELETE削除
│   │   │       ├── vitals/
│   │   │       │   └── route.ts    # GET一覧, POST作成
│   │   │       └── families/
│   │   │           ├── route.ts    # POST紐付け
│   │   │           └── [familyId]/
│   │   │               └── route.ts # DELETE紐付け解除
│   │   └── families/
│   │       ├── route.ts            # GET一覧, POST新規作成
│   │       └── [id]/
│   │           └── route.ts        # PATCH更新
│   └── layout.tsx
├── components/
│   ├── sidebar/
│   │   ├── Sidebar.tsx             # サイドバー全体
│   │   ├── CareRecipientList.tsx   # 介護対象者一覧
│   │   └── StatusBadge.tsx         # ステータスバッジ
│   ├── vitals/
│   │   ├── VitalDashboard.tsx      # バイタルダッシュボード
│   │   ├── VitalInputForm.tsx      # バイタル入力フォーム
│   │   ├── VitalChart.tsx          # 時系列グラフ
│   │   └── VitalCard.tsx           # バイタル値カード
│   ├── profile/
│   │   ├── ProfileView.tsx         # プロフィール表示
│   │   ├── ProfileEditForm.tsx     # プロフィール編集フォーム
│   │   └── FamilySection.tsx       # 家族一覧・管理
│   └── ui/                         # shadcn/ui コンポーネント
├── lib/
│   ├── prisma.ts                   # Prismaクライアント（シングルトン）
│   ├── auth.ts                     # NextAuth設定
│   ├── business-code.ts            # 事業者IDコード生成
│   ├── email.ts                    # 事業者ID送信メール（Nodemailer）
│   └── vitals-utils.ts             # バイタル正常範囲ロジック
├── prisma/
│   ├── schema.prisma
│   └── seed.ts                     # 初期データ投入
├── types/
│   └── index.ts                    # 共通型定義
└── docs/                           # 設計書
```

---

## 2. データベース設計

### 2.1 ER図

```
businesses ──< users ────────< vitals
     │
     └──────< care_recipients (削除済み除く)
                    │
         ┌──────────┼──────────┐
         │          │          │
  medical_conditions  allergies  vitals
                    │
         care_recipient_families（中間）
                    │
                families
```

※ `users` と `care_recipients` はそれぞれ `businesses` への必須外部キー `business_id` を持つ（マルチテナント分離）。

### 2.2 テーブル一覧

#### businesses（事業者）

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| id | VARCHAR(25) | PK | cuid() |
| code | VARCHAR(50) | UNIQUE, NOT NULL | 事業者ID（短い英数字コード、例 `BIZ-A7K2X9P4`） |
| company_name | VARCHAR(255) | NOT NULL | 会社名 |
| address | VARCHAR(255) | NOT NULL | 会社住所 |
| contact_name | VARCHAR(100) | NOT NULL | 担当者名 |
| phone | VARCHAR(20) | NOT NULL | 電話番号 |
| email | VARCHAR(255) | NOT NULL | メールアドレス（事業者ID送付先） |
| created_at | TIMESTAMP | NOT NULL | |
| updated_at | TIMESTAMP | NOT NULL | |

#### users（システムユーザ）

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| id | VARCHAR(25) | PK | cuid() |
| name | VARCHAR(100) | NOT NULL | 表示名 |
| email | VARCHAR(255) | UNIQUE, NOT NULL | |
| password_hash | VARCHAR(255) | NOT NULL | bcryptハッシュ |
| role | ENUM(Role) | NOT NULL, DEFAULT STAFF | |
| business_id | VARCHAR(25) | FK → businesses.id, NOT NULL | 所属事業者 |
| created_at | TIMESTAMP | NOT NULL | |
| updated_at | TIMESTAMP | NOT NULL | |

#### care_recipients（介護対象者）

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| id | VARCHAR(25) | PK | cuid() |
| name | VARCHAR(100) | NOT NULL | 氏名 |
| name_kana | VARCHAR(100) | NOT NULL | ふりがな |
| gender | ENUM(Gender) | NOT NULL | MALE/FEMALE/OTHER |
| birth_date | DATE | NOT NULL | |
| blood_type | ENUM(BloodType) | NULL | |
| room | VARCHAR(50) | NULL | 病室 |
| status | ENUM(CareStatus) | NOT NULL, DEFAULT HEALTHY | |
| notes | TEXT | NULL | 備考 |
| deleted_at | TIMESTAMP | NULL | 論理削除 |
| business_id | VARCHAR(25) | FK → businesses.id, NOT NULL | 所属事業者 |
| created_at | TIMESTAMP | NOT NULL | |
| updated_at | TIMESTAMP | NOT NULL | |

#### families（家族）

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| id | VARCHAR(25) | PK | cuid() |
| name | VARCHAR(100) | NOT NULL | 氏名 |
| relationship | VARCHAR(50) | NOT NULL | 続柄 |
| phone | VARCHAR(20) | NULL | 電話番号 |
| email | VARCHAR(255) | NULL | |
| notes | TEXT | NULL | |
| created_at | TIMESTAMP | NOT NULL | |
| updated_at | TIMESTAMP | NOT NULL | |

#### care_recipient_families（中間テーブル）

| カラム | 型 | 制約 |
|--------|-----|------|
| care_recipient_id | VARCHAR(25) | FK → care_recipients.id |
| family_id | VARCHAR(25) | FK → families.id |
| ※複合主キー(care_recipient_id, family_id) | | |

#### medical_conditions（持病）

| カラム | 型 | 制約 |
|--------|-----|------|
| id | VARCHAR(25) | PK |
| care_recipient_id | VARCHAR(25) | FK, NOT NULL |
| name | VARCHAR(200) | NOT NULL |
| created_at | TIMESTAMP | NOT NULL |

#### allergies（アレルギー）

| カラム | 型 | 制約 |
|--------|-----|------|
| id | VARCHAR(25) | PK |
| care_recipient_id | VARCHAR(25) | FK, NOT NULL |
| name | VARCHAR(200) | NOT NULL |
| created_at | TIMESTAMP | NOT NULL |

#### vitals（バイタル記録）

| カラム | 型 | 制約 | 単位 |
|--------|-----|------|------|
| id | VARCHAR(25) | PK | |
| care_recipient_id | VARCHAR(25) | FK, NOT NULL | |
| recorded_at | TIMESTAMP | NOT NULL | |
| recorded_by | VARCHAR(25) | FK → users.id, NOT NULL | |
| systolic_bp | INTEGER | NULL | mmHg |
| diastolic_bp | INTEGER | NULL | mmHg |
| heart_rate | INTEGER | NULL | bpm |
| respiratory_rate | INTEGER | NULL | 回/分 |
| temperature | DECIMAL(4,1) | NULL | ℃ |
| spo2 | INTEGER | NULL | % |
| weight | DECIMAL(5,2) | NULL | kg |
| blood_sugar | INTEGER | NULL | mg/dL |
| consciousness_level | INTEGER | NULL | JCS |
| pain_score | INTEGER | NULL | NRS 0-10 |
| edema | ENUM(Edema) | NULL | |
| urine_output | INTEGER | NULL | mL |
| input_method | ENUM(InputMethod) | NOT NULL, DEFAULT MANUAL | |
| notes | TEXT | NULL | |
| created_at | TIMESTAMP | NOT NULL | |

### 2.3 ENUM定義

```
Gender:      MALE | FEMALE | OTHER
BloodType:   A_PLUS | A_MINUS | B_PLUS | B_MINUS | O_PLUS | O_MINUS | AB_PLUS | AB_MINUS
CareStatus:  CRITICAL | SEVERE | CAUTION | OBSERVATION | HEALTHY | DECEASED | DISCHARGED
Edema:       NONE | MILD | MODERATE | SEVERE
InputMethod: MANUAL | WEARABLE
Role:        ADMIN | STAFF
```

---

## 3. 画面設計

### 3.1 画面一覧

| 画面ID | 画面名 | URL | 認証 |
|--------|--------|-----|------|
| SCR-001 | ログイン | /login | 不要 |
| SCR-002 | ダッシュボード | / | 必要 |
| SCR-003 | プロフィール表示 | /care-recipients/[id] | 必要 |
| SCR-004 | プロフィール編集 | /care-recipients/[id]/edit | 必要 |
| SCR-005 | 介護対象者新規登録 | /care-recipients/new | 必要 |
| SCR-006 | バイタル入力 | /care-recipients/[id]/vitals/new | 必要 |
| SCR-007 | 事業者登録 | /register/business | 不要 |
| SCR-008 | アカウント新規登録 | /register/account | 不要 |

### 3.2 ナビゲーションフロー

```
/login
  ├→ [事業者登録リンク] → /register/business
  │       └→ 事業者ID発行（メール送信） → /register/account へ案内
  ├→ [アカウント新規登録リンク] → /register/account
  │       └→ 登録成功 → /login?registered=1 へ戻る
  └→ / （ダッシュボード）
       ├→ サイドバーで対象者クリック → バイタル表示（メインエリア更新）
       │     ├→ [プロフィール表示ボタン] → /care-recipients/[id]
       │     │     └→ [編集ボタン] → /care-recipients/[id]/edit
       │     └→ [バイタル入力ボタン] → /care-recipients/[id]/vitals/new
       └→ [対象者追加ボタン] → /care-recipients/new
```

---

## 4. API設計

### 4.0 事業者・アカウント登録 API（認証不要）

| メソッド | パス | 説明 | ボディ |
|---------|------|------|--------|
| POST | /api/businesses | 事業者登録。事業者IDを発行しメール送信 | { companyName, address, contactName, phone, email } |
| POST | /api/register | アカウント新規登録。事業者の最初の登録者はADMIN、以降STAFF | { businessCode, email, name, password, passwordConfirm } |

- `/api/businesses`: 一意な `code` を生成して `businesses` を作成、`sendBusinessIdEmail()` で事業者IDを送付し、`code` をレスポンスに含める。
- `/api/register`: `businessCode` から事業者を特定（不在は404）、メール重複は409、パスワード不一致は400。所属事業者のユーザ数が0ならADMIN、それ以外はSTAFF。

### 4.1 介護対象者 API

| メソッド | パス | 説明 | ボディ |
|---------|------|------|--------|
| GET | /api/care-recipients | 一覧取得（削除済除く） | - |
| POST | /api/care-recipients | 新規作成 | CareRecipientInput |
| GET | /api/care-recipients/:id | 詳細取得 | - |
| PATCH | /api/care-recipients/:id | 更新 | Partial<CareRecipientInput> |
| DELETE | /api/care-recipients/:id | 論理削除 | - |

### 4.2 バイタル API

| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/care-recipients/:id/vitals | 一覧取得（最新100件） |
| POST | /api/care-recipients/:id/vitals | 記録作成 |
| DELETE | /api/care-recipients/:id/vitals/:vitalId | 削除 |

### 4.3 家族 API

| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/families | 全家族一覧取得 |
| POST | /api/families | 家族新規作成 |
| PATCH | /api/families/:id | 家族情報更新 |
| POST | /api/care-recipients/:id/families | 介護対象者へ家族を紐付け |
| DELETE | /api/care-recipients/:id/families/:familyId | 紐付け解除 |

### 4.4 テナント分離方針

- 認証済みAPI／サーバコンポーネントの `users`・`care_recipients` への読み書きは、すべてセッションの `businessId` で絞り込む（一覧は `where: { businessId }`、作成は `data` に付与、個別取得・更新・削除は所属事業者一致を確認し、不一致は404）。
- `session.user.businessId` は NextAuth の jwt / session コールバックで伝播する。

---

## 5. ウェアラブルデバイス連携設計（将来実装）

### 5.1 技術方式
- **プロトコル**: Bluetooth Low Energy (BLE)
- **API**: Web Bluetooth API（ブラウザネイティブ）
- **必要条件**: HTTPS環境、Chrome系ブラウザ

### 5.2 対応デバイスプロファイル
- Health Thermometer Profile (体温計)
- Heart Rate Profile (心拍計)
- Blood Pressure Profile (血圧計)
- Pulse Oximeter Profile (SpO2計)

### 5.3 実装方針
1. バイタル入力フォームに「デバイスから取得」ボタンを配置（現状はdisabled）
2. クリック時にWeb Bluetooth APIでデバイスをスキャン
3. 取得値をフォームに自動入力（ユーザが確認後に保存）
4. `input_method = WEARABLE` で記録
